name: Empty Workflow

on:
  push:
    branches:
      - main

jobs:
  empty-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Add any additional steps or commands as needed

